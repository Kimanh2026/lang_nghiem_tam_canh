import 'package:flutter/material.dart';

class AppPalette {
  const AppPalette._();

  /// One shared glass opacity for cards, bars, dialogs and input surfaces.
  static const glassPanel = Color(0xB81B2D38);
  static const glassPanelHighlight = Color(0xB8465964);
}
